module.exports = function(context) {
    /*const Q = context.requireCordovaModule("q");
    const events = context.requireCordovaModule('cordova-common').events;
    const deferral = Q.defer();

    // events.emit('info', 'Testing beforePrepare hook');

    console.log('test');

    setTimeout(() => {
        console.log('running some task');
        deferral.resolve();
    }, 3000);
    
    return deferral.promise;*/
};